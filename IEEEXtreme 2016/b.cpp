#include <bts/stdc++.h>

using namespace std;

int count_total(int)

int main() {
	

	int t;
	cin >> t;
	while(t--) {

		int q,l;
		cin >> q >> l;

		string cont;
		for(int i = 0; i < q; i++) {
			do {
				string tp; cin >> tp;
				string a1; cin >> a1;
				string a2; cin >> a2;



			} while (cin >> cont, (cont == 'and' || cont == 'or'));

		}
	}
}